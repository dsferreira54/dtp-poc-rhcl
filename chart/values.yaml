gwapiDomain: gwapi.ocp.acme.com
privategwDomain: privategw.ocp.acme.com
ingressDomain: apps.ocp.acme.com
rhclVersion: 1.4.1
namespaces:
  exampleApps: example-apps
  externalAppConsumers: external-app-consumers
  rhbk: rhbk
# IAM externo (OIDC). Com enabled: false usa o Keycloak interno (realm rhcl).
externalIAM:
  enabled: false
  parameters:
    issuer: https://hiam.gerid.acme.com/cas/oidc
    jwks: https://hiam.gerid.acme.com/cas/oidc/jwks
    authorize: https://hiam.gerid.acme.com/cas/oidc/authorize
    accessToken: https://hiam.gerid.acme.com/cas/oidc/accessToken
rhbk:
  helloWorldApp:
    clientId: hello-world-app
    clientSecret: hello-world-app-secret
  redApp:
    clientId: red-app
    clientSecret: red-app-secret
monitoring:
  userWorkload:
    enabled: true
  rhclMetrics:
    enabled: true
metallb:
  enabled: true
  ipAddressPool:
    addresses:
      - 192.168.1.240-192.168.1.250
ingress:
  selfSigned:
    enabled: false
    jobNamespace: ingress-selfsigned
    secretNamespace: openshift-ingress
    secretName: cert-manager-ingress-cert
    certificateName: cert-manager-ingress-cert
    routerNamespace: openshift-ingress
    routerDeployment: router-default
    validityDays: 90
htpasswd:
  enabled: false
  jobNamespace: ingress-selfsigned
  username: demo
  passwordHash: "$2y$05$LafNcbjVdORYJGDqvQ99x.KJKiI9jz7sMEuz9lpAHMCK868d3cnaK"
  secretName: htpasswd-demo
  identityProviderName: htpasswd-demo
  clusterAdminBindingName: htpasswd-demo-cluster-admin
authorino:
  trustIngressCA:
    enabled: false
    ingressSecretNamespace: openshift-ingress
    ingressSecretName: router-certs-default